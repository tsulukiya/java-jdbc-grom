package lesson3.homeWork.part3;import java.sql.*;public class Solution {    private static final String DB_URL = "jdbc:oracle:thin:@gromcode-lessons.clg2f8ljlc7q.us-east-2.rds.amazonaws.com:1521:ORCL";    private static final String USER = "main";    private static final String PASS = "TsulukiyA_123";    public static void main(String[] args) {        Solution solution = new Solution();        //method testSavePerformance() speed - 136463 ms;        //System.out.println(testSpeedDb.testSavePerformance());        //method testDeleteByIdPerformance() speed - 138441 ms;        //System.out.println(testSpeedDb.testDeleteByIdPerformance());        //method testDeletePerformance() speed - 2259 ms;        //System.out.println(testSpeedDb.testDeletePerformance());        //method testSelectByPerformance() speed - 140554 ms;        //System.out.println(testSpeedDb.testSelectByIdPerformance());        //method testSelectPerformance() speed - 2146 ms;        //System.out.println(testSpeedDb.testSelectPerformance());    }    public long testSavePerformance() {        long start = System.currentTimeMillis();        try (Connection connection = getConnection()) {            for (int i = 0; i < 1000; i++) {                try (PreparedStatement preparedStatement = connection.prepareStatement("INSERT INTO TEST_SPEED VALUES (?, ?, ?)")) {                    preparedStatement.setLong(1, 1000 + i);                    preparedStatement.setString(2, "toy" + i);                    preparedStatement.setInt(3, i);                    preparedStatement.executeUpdate();                } catch (SQLException e) {                    System.err.println("Something went wrong");                    e.printStackTrace();                }            }        } catch (SQLException e) {            System.err.println("Something went wrong");            e.printStackTrace();        }        return System.currentTimeMillis() - start;    }    public long testDeleteByIdPerformance() {        long start = System.currentTimeMillis();        try (Connection connection = getConnection()) {            for (int i = 0; i < 1000; i++) {                try (PreparedStatement preparedStatement = connection.prepareStatement("DELETE FROM TEST_SPEED WHERE ID=?")) {                    preparedStatement.setLong(1, 1000 + i);                    preparedStatement.executeUpdate();                } catch (SQLException e) {                    System.err.println("Something went wrong");                    e.printStackTrace();                }            }        } catch (SQLException e) {            System.err.println("Something went wrong");            e.printStackTrace();        }        return System.currentTimeMillis() - start;    }    public long testDeletePerformance() {        long start = System.currentTimeMillis();        try (Connection connection = getConnection(); Statement statement = connection.createStatement()) {            int m = statement.executeUpdate("DELETE FROM TEST_SPEED");            System.out.println(m);        } catch (SQLException e) {            System.err.println("Something went wrong");            e.printStackTrace();        }        return System.currentTimeMillis() - start;    }    public long testSelectByIdPerformance() {        long start = System.currentTimeMillis();        try (Connection connection = getConnection()) {            for (int i = 0; i < 1000; i++) {                try (PreparedStatement preparedStatement = connection.prepareStatement("SELECT * FROM TEST_SPEED WHERE ID=?")) {                    preparedStatement.setLong(1, 1000 + i);                    preparedStatement.executeUpdate();                } catch (SQLException e) {                    System.err.println("Something went wrong");                    e.printStackTrace();                }            }        } catch (SQLException e) {            System.err.println("Something went wrong");            e.printStackTrace();        }        return System.currentTimeMillis() - start;    }    public long testSelectPerformance() {        long start = System.currentTimeMillis();        try (Connection connection = getConnection(); Statement statement = connection.createStatement()) {            statement.executeQuery("SELECT * FROM TEST_SPEED");        } catch (SQLException e) {            System.err.println("Something went wrong");            e.printStackTrace();        }        return System.currentTimeMillis() - start;    }    private Connection getConnection() throws SQLException {        return DriverManager.getConnection(DB_URL, USER, PASS);    }}